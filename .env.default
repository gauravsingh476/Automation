# Default environment configuration
# Used when .env.local is not present (e.g. CI, shared environments)
# Values can be hardcoded here or injected as real environment variables at runtime

VERSION=pre_prod
EMAIL=
PASSWORD=
